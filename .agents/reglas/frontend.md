# Frontend Rules

Apply these rules for UI/client-facing behavior.

- Keep UI concerns separate from domain logic.
- Reuse existing design patterns and shared primitives before adding new ones.
- Avoid introducing visual or UX complexity unless explicitly requested.
- Add tests for new user-visible interactions when applicable.
